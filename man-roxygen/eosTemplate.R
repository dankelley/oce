#' @param eos Character indication of the seawater equation of state
#' to use. The permitted choices are \code{"gsw"} and \code{"unesco"}.
#' If \code{eos} is not supplied, it defaults to
#' \code{getOption("oceEOS", default="gsw")}.

