#' @param eos Character indication of the seawater equation of state
#' to use. The permitted choices are `"gsw"` and `"unesco"`. If `eos` is
#' not supplied, it defaults to [`getOption`]`("oceEOS",default="gsw")`.

