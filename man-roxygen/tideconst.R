#' To see the permitted constituent names and their frequencies
#' (in cycles per hour), use:
#'\preformatted{
#' data(tidedata)
#' data.frame(name=tidedata$const$name, freq=tidedata$const$freq)
#'}

